# Platform Schema

Schema vật lý: `platform` — 42 bảng.
Dữ liệu bảo mật lõi của SaaS, không bao giờ được export kèm tenant.

| Bảng | Chức năng | → Tham chiếu (FK ra) | ← Được tham chiếu (FK vào) |
| --- | --- | --- | --- |
| `mst_platform_permission` | Danh mục các khóa quyền (Permission) quản trị hệ thống (VD: Tạo tenant, quản lý API key, backup, maintenance mode); hỗ trợ cây menu/action/data scope cho Platform Admin. | — | `mst_platform_permission_role`, `mst_platform_permission` (self) |
| `mst_platform_role` | Lưu danh sách các vai trò (Role) của hệ thống Platform (VD: Super Admin, Operator, Security Admin). | — | `mst_platform_user`, `mst_platform_permission_role` |
| `mst_platform_permission_role` | Bảng Pivot (Junction) ánh xạ quan hệ N-N giữa Role và Permission của cấp Platform. | `mst_platform_role`, `mst_platform_permission` | — |
| `mst_platform_user` | Tài khoản quản trị viên cấp Platform (vận hành Carima/JustFine), tách biệt với người dùng tenant; lưu trạng thái lock, fail count, SSO placeholder và dấu vết đổi mật khẩu. | `mst_platform_role` | `cfg_platform_api_key`, `tmp_platform_pwd_reset_token`, `t_platform_login_history`, `t_platform_auth_session`, `t_moto_saki_relation_history`, `t_tenant_provision_log`, `t_platform_backup_job`, `t_platform_audit_log` |
| `tmp_platform_pwd_reset_token` | Lưu token quên mật khẩu của Platform Admin (có thời hạn). | `mst_platform_user` | — |
| `t_platform_login_history` | Lịch sử login attempt của Platform Admin, bao gồm thành công/thất bại/lock, IP, user-agent, request_id và correlation_id để phục vụ audit/SIEM. | `mst_platform_user` | — |
| `t_platform_auth_session` | Phiên đăng nhập/refresh token hash của Platform Admin theo enterprise short session policy; DB là source of truth, Redis chỉ là cache/denylist. | `mst_platform_user` | — |
| `t_platform_user_2fa` | Trạng thái 2FA (email-code + backup code) của Platform Admin; 2FA opt-in (future/optional). | `mst_platform_user` | `t_platform_2fa_backup_code` (logic) |
| `t_platform_2fa_backup_code` | Backup code 2FA của Platform Admin, dùng một lần và tự làm mới cả bộ sau khi dùng. | `mst_platform_user` | — |
| `cfg_platform_tenant_feature` | Bật/tắt module tính năng theo từng tenant (module enablement, không phải paid plan). | `mst_tenant_registry` | — |
| `mst_tenant_registry` | **[M09 Modified]** Sổ đăng ký tenant. Thêm: health_status (G/Y/R cached cho F-1402), last_activity_at (BR-143), last_health_check_at, last_suspension_reason, reactivation_count, sla_tier (BRONZE-PLATINUM), oem_provider_id (Future-proof White-label nullable). | — | `mst_moto_saki_relation`, `cfg_platform_tenant_setting`, `cfg_platform_secret_reference`, `cfg_platform_api_key`, `t_tenant_provision_log`, `t_tenant_lifecycle_history`, `t_tenant_snapshot`, `cfg_platform_backup_policy`, `t_platform_restore_execution`, `cfg_feature_flag_assignment`, `t_batch_job_tenant_execution`, `mst_tenant_registry` (self via oem_provider_id) |
| `cfg_platform_tenant_setting` | Lưu cấu hình tùy biến cho từng Tenant (Domain, Logo, Màu sắc, Bật/tắt module tính năng). | `mst_tenant_registry` | — |
| `cfg_platform_secret_reference` | Metadata tham chiếu tới secret lưu ngoài DB (AWS Secrets Manager) cho tenant/platform; chỉ lưu secret path/ARN, purpose, trạng thái rotation, không lưu giá trị secret. | `mst_tenant_registry` | `mst_tenant_registry` |
| `cfg_platform_api_key` | Metadata và hash của API key riêng theo tenant; chỉ lưu key prefix, key hash, scope, IP allowlist, status, thời hạn và trạng thái rotation/revoke, không lưu raw secret. | `mst_tenant_registry` | — |
| `t_tenant_provision_log` | **[M09 Modified]** Nhật ký cấp phát/thay đổi tenant. Thêm: step_sequence (multi-step workflow), expected/actual_duration_ms (SLA), rollback_status (REQUESTED→IN_PROGRESS→COMPLETED/FAILED), retry_count + next_retry_at (BR-187 exponential backoff). | `mst_tenant_registry`, `mst_platform_user` | `t_tenant_lifecycle_history` (logical link) |
| `mst_moto_saki_relation` | Bản đồ quan hệ N-N giữa MOTO và SAKI ở cấp Platform; là nguồn kiểm tra quan hệ active để định tuyến yêu cầu phái cử và kiểm soát truy cập cross-tenant. | `mst_tenant_registry` | `t_moto_saki_relation_history` |
| `t_moto_saki_relation_history` | Lịch sử thay đổi trạng thái quan hệ MOTO-SAKI (active/terminated/restore), lưu thời điểm, người thao tác và lý do để vẫn truy vết được quan hệ đã chấm dứt. | `mst_moto_saki_relation`, `mst_platform_user` | — |
| `cfg_platform_system_setting` | Cấu hình hệ thống cấp Platform như system parameter, maintenance mode, mail/storage metadata, backup retention; không lưu secret value. | — | — |
| `mst_system_config` | **[M07 NEW]** Tham số hệ thống dùng chung (~30 keys: overtime_monthly_limit_hours, max_dispatch_years, session_timeout, ...). is_tenant_overridable=0 cho tham số pháp luật (3年抵触日, cooling_period). | — | — |
| `t_master_import_history` | **[M07 NEW]** Lịch sử Platform Admin import hàng loạt master data (CSV/XLSX, ≤5000 rows, all-or-nothing). File gốc lưu S3 ≥1 năm; error chi tiết trong error_detail_payload (JSONB). | `mst_platform_user` | — |
| `mst_notification_template` | **[M08 NEW]** Template JP (Mustache subject/body) cho 30+ NTF types (NTF-C01...NTF-N02). Platform quản lý tập trung (BR-131/133); tenant không sửa nội dung — chỉ cấu hình kênh email/in-app on/off. is_legal_alert=1 cho cảnh báo pháp lý (36協定/抵触日/treatment) — không thể tắt in-app. | — | `t_saki_notification_log` (logical), `t_moto_notification_log` (logical) |
| `t_tenant_lifecycle_history` | **[M09 NEW]** Audit chuyên dụng cho status transitions của tenant. Tách khỏi `t_tenant_provision_log` (action-centric) — bảng này state-centric. Ghi old/new_status, transition_type (AUTO_PROVISION/MANUAL_ADMIN/AUTO_SUSPEND/AUTO_FAIL), duration_in_previous_state_seconds để phân tích tenant churn. Immutable. | `mst_tenant_registry`, `t_tenant_provision_log` | — |
| `t_system_config_history` | **[M09 NEW]** Versioning lịch sử thay đổi `mst_system_config` (BR-152). UNIQUE (config_id, version_number). is_rollback flag + rollback_target_version. change_reason. Pure-immutable insert-only. | `mst_system_config`, `mst_platform_user` | — |
| `cfg_platform_backup_policy` | **[M09 NEW]** Chính sách sao lưu per-tenant (BR-14D): frequency (DAILY/WEEKLY/MONTHLY), retention_days, rto/rpo target (BR-14H), encryption_required_flg, cron_expression, s3_destination_prefix. tenant_id NULL = policy mặc định. | `mst_tenant_registry` | `t_platform_backup_job` |
| `t_tenant_snapshot` | **[M09 NEW]** Snapshot ad-hoc safety net trước thao tác nguy hiểm (BR-14A SUSPEND / BR-14F RESTORE / BR-14J MIGRATION). Tách khỏi `t_platform_backup_job` (định kỳ). retention_expires_at default 7 ngày — batch cleanup. Hỗ trợ rds_snapshot_id (RDS-native) hoặc s3_backup_path (logical). | `mst_tenant_registry`, `mst_platform_user` | `t_platform_restore_execution` |
| `t_platform_restore_execution` | **[M09 NEW]** Critical safety log cho restore. BR-14E dual_confirm_token, BR-14F rollback_snapshot_id BẮT BUỘC khi restore_mode=OVERWRITE (CHECK constraint enforce). integrity_check_status sau COMPLETED. Source: backup_job hoặc snapshot. | `mst_tenant_registry`, `t_platform_backup_job`, `t_tenant_snapshot`, `mst_platform_user` | — |
| `t_platform_maintenance_window` | **[M09 NEW]** Khung giờ bảo trì (F-1409): SCHEDULED (CHECK ≥48h BR-14P) / EMERGENCY (BR-14Q), scope ALL/SPECIFIC/PLATFORM_ONLY, impact_level NO_IMPACT/DEGRADED/FULL_DOWNTIME, planned/actual_start_end, notification_sent_at_payload (JSONB audit). | `mst_platform_user` | — |
| `mst_feature_flag` | **[M09 NEW]** Catalog feature flag (F-1412): flag_type RELEASE/EXPERIMENT/OPERATIONAL/KILL_SWITCH. KILL_SWITCH CHECK bypass cache (cache_ttl_seconds=0 — BR-14Z). rollout_percentage (gradual), scheduled_enable/disable_at. Phân biệt với `cfg_platform_tenant_feature` (module on/off coarse-grained). | — | `cfg_feature_flag_assignment` |
| `cfg_feature_flag_assignment` | **[M09 NEW]** Per-tenant override cho feature flag: assignment_type INDIVIDUAL (1 tenant)/GROUP (MOTO/SAKI)/PERCENTAGE. CHECK chk_group_consistency ép tenant_id ↔ group_tenant_type theo type. enabled_by_platform_user_id audit. | `mst_feature_flag`, `mst_tenant_registry`, `mst_platform_user` | — |
| `mst_batch_job` | **[M09 NEW]** Catalog 11 batch jobs (F-1425/1428): job_type ACCOUNTING/BILLING/MONITORING/MAINTENANCE/NOTIFICATION/DATA_RETENTION/BACKUP, cron + timezone (default Asia/Tokyo), retry_policy NO_RETRY/AUTO_RETRY (3 lần BR-187) /MANUAL, max_concurrent_tenants (BR-191), critical_flg (dual-confirm BR-193). | — | `t_batch_job_execution`, `t_batch_job_tenant_execution` |
| `t_batch_job_execution` | **[M09 NEW]** Platform-level summary per execution: status RUNNING/SUCCESS/FAILED/TIMEOUT/SKIPPED/MANUALLY_RESOLVED, total/succeeded/failed/skipped_tenants, retry_log_payload JSONB, triggered_by SCHEDULED/MANUAL/AUTO_RETRY/RETRY_AFTER_SUSPEND (BR-14C catch-up). | `mst_batch_job`, `mst_platform_user` | `t_batch_job_tenant_execution` |
| `t_batch_job_tenant_execution` | **[M09 NEW]** Per-tenant detail (BR-183 isolation): UNIQUE (execution_id, tenant_id, retry_attempt). error_type DB_ERROR/TIMEOUT/DATA_ERROR/UNKNOWN, skip_reason BẮT BUỘC khi SKIPPED (CHECK BR-190). Partial IDX status=FAILED cho F-1427 retry view. | `t_batch_job_execution`, `mst_batch_job`, `mst_tenant_registry` | — |
| `t_evidence_timestamp` | **[M10 NEW]** Universal TSA (Time Stamp Authority) registry — polymorphic association (entity_type, entity_tenant_id, entity_id). BR-1554 SHA-256+ only (CHECK cấm MD5/SHA-1), BR-1553 TSA phải được 日本データ通信協会 公認, BR-1555 gap-free chain via previous_timestamp_id + chain_sequence UNIQUE. Áp dụng cho 36協定 PDF, ledger export, contract, invoice, treatment notification. | (polymorphic, logical) | (referenced by tenant entities) |
| `t_compliance_violation` | **[M10 NEW]** Central tracking 14 loại vi phạm VIO-01...VIO-14 cross-tenant (CMP-018). severity (1=Low/2=Medium/3=High/4=Critical), status 5 states (未対応/対応中/是正済み/クローズ/棄却), escalation_level 1-3, deadline BR-1567/1568/1569 (24h/3d/7d). CHECK chk_critical_rca BR-1574 + chk_dismissal_dual_approval BR-1571. Immutable BR-1570/1575. | `mst_platform_user` (multiple FKs) | `t_compliance_violation_timeline` |
| `t_compliance_violation_timeline` | **[M10 NEW]** Immutable timeline audit per violation (BR-1572). 11 event_type (CREATED/STATUS_CHANGED/ESCALATION_UP/DOWN/ASSIGNED/REASSIGNED/NOTE_ADDED/EVIDENCE_ADDED/DISMISSAL_REQUESTED/APPROVED/REJECTED). Pure-immutable insert-only — BR-1575 vĩnh viễn. | `t_compliance_violation`, `mst_platform_user` | — |
| `t_ledger_export_history` | **[M10 NEW]** Cross-tenant audit lịch sử xuất 派遣先/派遣元管理台帳 (CMP-012 BR-1545 watermark, BR-1547 audit). CHECK chk_pdf_consistency: PDF format BẮT BUỘC kèm S3 storage + SHA-256 hash + evidence_timestamp_id (Q1 user decision — immutable PDF snapshot trên S3 giữ 12年4ヶ月 per 電子帳簿保存法 vượt trên retention 3 năm của bảng). | `mst_platform_user`, `t_evidence_timestamp` | — |
| `t_platform_notification_history` | **[M08 Modified]** Re-purpose: CHỈ phục vụ お知らせ broadcast (Announcement) Platform → Tenants. Event-driven noti chuyển sang `t_*_notification_log` ở tenant schema. Thêm: notification_kind/category/priority/status (DRAFT/SCHEDULED/PUBLISHED/EXPIRED/WITHDRAWN)/target_scope/publish_at/expires_at/send_email_flg/attachments_payload/target_tenant_ids_payload. | — | `t_saki_notification_read` (logical FK), `t_moto_notification_read` (logical FK), `t_staff_notification_read` (logical FK) |
| `t_platform_backup_job` | **[M09 Modified]** Lịch sử job backup. Thêm: backup_policy_id (FK → cfg_platform_backup_policy), incremental_flg, backup_size_mb (dashboard F-1404), retention_days (snapshot), encryption_algorithm, verification_status (PENDING/PASSED/FAILED/SKIPPED — BR-14J), rto/rpo_minutes (BR-14H). | `mst_platform_user`, `cfg_platform_backup_policy` | `t_platform_restore_execution` |
| `t_platform_audit_log` | **[M09 Modified]** Audit log Platform Admin. Thêm: severity_level (INFO/WARNING/CRITICAL — BR-165 dashboard filter), change_category (IAM/BILLING/OPERATIONS/SECURITY/CONFIG), affected_tenant_count, approval_status (NULL/PENDING/APPROVED/REJECTED cho dual-approval workflow BR-14E). | — | — |
| `t_platform_inbox_event` | Event Platform nhận từ SAKI, MOTO hoặc hệ thống ngoài. Cấu trúc lưu source_tenant_type, source_tenant_id (0 = SYSTEM hoặc các hệ thống EXT_* như EXT_STRIPE, EXT_BANK, EXT_LEGACY). UNIQUE theo source_type, source_id, idempotency_key để chống trùng lặp tuyệt đối. | — | — |
| `t_platform_outbox_event` | Event Platform phát sinh để gửi đi. Cấu trúc lưu target_tenant_type, target_tenant_id (0 = BROADCAST/SYSTEM). UNIQUE theo target_type, target_id, idempotency_key. | — | — |
| `t_tenant_usage_metric` | Đo lường lượng sử dụng (User, Hợp đồng, Dung lượng) hàng tháng của các Tenant. | `mst_tenant_registry` | — |

---