# MOTO Schema

Schema vật lý: `tenant_moto_<id>` — 116 bảng.

| Bảng | Chức năng | → Tham chiếu (FK ra) | ← Được tham chiếu (FK vào) |
| --- | --- | --- | --- |
| `tmp_moto_setup_credential` | Bảng tạm lưu thông tin khởi tạo tài khoản MOTO khi onboarding (dùng một lần). | — | — |
| `mst_moto_company` | MOTO / 派遣元: công ty cung ứng nhân sự phái cử, là bên quản lý staff, hợp đồng, chấm công, hóa đơn. | — | `cfg_moto_36_agreement`, `cfg_moto_36_agreement_free`, `cfg_moto_36_overtime_start`, `cfg_moto_advance_payment_screen`, `cfg_moto_alarm_setting`, `cfg_moto_alarm_recipient`, `cfg_moto_alarm_conflict`, `cfg_moto_bank_account`, `cfg_moto_client_company`, `cfg_moto_compensatory_holiday`, `cfg_moto_contract_additional_text`, `cfg_moto_contract_default`, `cfg_moto_contract_party`, `cfg_moto_contract_text_per_client`, `cfg_moto_invoice_calculation`, `cfg_moto_invoice_default`, `cfg_moto_invoice_fraction`, `cfg_moto_legal_holiday`, `cfg_moto_seal`, `mst_moto_attendance_category`, `mst_moto_company_contact`, `mst_moto_contract_company`, `mst_moto_department`, `mst_moto_office`, `mst_moto_shift`, `mst_moto_user`, `mst_staff`, `t_contract`, `t_invoice`, `t_invoice_approval`, `t_invoice_status_history`, `t_invoice_upload_history`, `t_moto_batch_execution_history`, `t_treatment_notification`, `t_moto_contract_upload_history`, `t_attendance_email_history`, `t_document_download_audit`, `t_moto_audit_log`, `t_moto_outbox_event`, `t_moto_inbox_event` |
| `mst_moto_company_contact` | Thông tin liên hệ của công ty MOTO. | `mst_moto_company` | — |
| `mst_moto_office` | Văn phòng/chi nhánh của MOTO. | `mst_moto_company` | `mst_moto_user` |
| `mst_moto_department` | Phòng ban của MOTO. | `mst_moto_company` | `mst_moto_user` |
| `mst_moto_user` | Người dùng thuộc MOTO; gắn công ty, văn phòng, phòng ban và role để kiểm soát quyền chức năng/scope dữ liệu; hỗ trợ self-service forgot password, lock sau lỗi đăng nhập và SSO placeholder. | `mst_moto_company`, `mst_moto_office`, `mst_moto_department`, `mst_moto_role` | `cfg_moto_client_company_user`, `cfg_moto_alarm_recipient`, `tmp_moto_pwd_reset_token`, `mst_moto_user_scope`, `t_moto_login_history`, `t_moto_auth_session`, `t_staff_password_reset_request`, `t_moto_contract_upload_history`, `t_moto_batch_execution_history`, `t_invoice_upload_history`, `t_invoice_status_history`, `t_moto_audit_log`, `t_moto_notification_read` |
| `tmp_moto_pwd_reset_token` | Lưu token quên mật khẩu của người dùng MOTO. | `mst_moto_user` | — |
| `mst_moto_role` | Danh sách vai trò trong phạm vi một tenant MOTO, dùng để gán bộ quyền chức năng cho user MOTO. | — | `mst_moto_user`, `mst_moto_permission_role` |
| `mst_moto_permission` | Danh mục quyền chức năng cố định áp dụng cho màn hình/nghiệp vụ phía MOTO; hỗ trợ cây menu/action/data scope và mapping mã quyền e-staffing nếu cần import. | — | `mst_moto_permission_role`, `mst_moto_permission` (self) |
| `mst_moto_permission_role` | Bảng Pivot map quan hệ N-N, định nghĩa Role nào được chứa những quyền gì. | `mst_moto_role`, `mst_moto_permission` | — |
| `mst_moto_user_scope` | Phạm vi dữ liệu mà user MOTO được phép truy cập, ví dụ theo office, department, client company, staff phụ trách hoặc toàn công ty; scope được diễn giải bởi service layer, không bắt buộc FK vật lý tới từng scope target. | `mst_moto_user` | — |
| `cfg_moto_file_layout` | Tùy biến cột & thứ tự cột file DL/UL (契約/請求 CSV); 1 layout/loại/công ty, áp dụng toàn user. | `mst_moto_company` | — |
| `cfg_moto_editable_user_field` | Cấu hình các trường mà user MOTO được tự sửa trong màn hình thông tin user (ユーザー編集可能項目). | `mst_moto_company` | — |
| `t_moto_user_2fa` | Trạng thái 2FA (email-code + backup code) của user MOTO; opt-in. | `mst_moto_company`, `mst_moto_user` | `t_moto_2fa_backup_code` (logic) |
| `t_moto_2fa_backup_code` | Backup code 2FA của user MOTO, dùng một lần, tự làm mới cả bộ sau khi dùng. | `mst_moto_company`, `mst_moto_user` | — |
| `mst_moto_attendance_category` | Master phân loại chấm công của MOTO (đi làm, nghỉ, ca bù...). | `mst_moto_company` | `t_attendance_daily` |
| `mst_moto_workplace_type` | Master phân loại nơi làm việc (就業場所区分), định nghĩa các địa điểm như Văn phòng, Tại nhà, Vệ tinh để nhân viên chọn khi khai báo chấm công. | `mst_moto_company` | `t_attendance_daily` |
| `cfg_moto_client_company` | Master/cấu hình khách hàng SAKI mà MOTO có quan hệ giao dịch; saki_tenant_id là logical routing key, saki_company_code là business snapshot/display code. Bắt buộc validate qua quan hệ MOTO-SAKI active ở Platform. | `mst_moto_company` | `cfg_moto_client_company_user`, `cfg_moto_contract_party`, `cfg_moto_contract_text_per_client`, `cfg_moto_seal_per_client`, `t_contract`, `t_invoice`, `t_treatment_notification` |
| `cfg_moto_client_company_user` | Assignment nghiệp vụ: user MOTO nào phụ trách khách hàng SAKI nào. Dùng cho phân công xử lý, filter nghiệp vụ, người phụ trách giao dịch. | `cfg_moto_client_company`, `mst_moto_user` | — |
| `t_dispatch_inquiry_received` | Read model yêu cầu phái cử MOTO nhận từ SAKI. Quản lý theo chuẩn Immutable Revisions. Có saki_inquiry_revision_no. Khi SAKI sửa yêu cầu, hệ thống sinh dòng mới và set source status dòng cũ thành SUPERSEDED (trỏ superseded_by_id). Bảo vệ bằng Active Guard Unique Index. Dùng idempotency_key chống duplicate. | `mst_moto_company`, `cfg_moto_client_company` | `t_dispatch_estimate_response` |
| `t_dispatch_estimate_response` | Báo giá/phản hồi của MOTO cho yêu cầu. Áp dụng Immutable Versioning: Quản lý theo chuỗi estimate_group_id và version. Khi submit version mới, bản cũ tự động chuyển thành SUPERSEDED. Lưu log superseded_by_id để truy vết toàn vẹn lịch sử. | `t_dispatch_inquiry_received`, `mst_moto_user`, `t_dispatch_estimate_response` (self) | `t_dispatch_estimate_staff_profile` |
| `t_dispatch_estimate_staff_profile` | Profile ẩn danh đính kèm khi MOTO gửi Báo giá (Estimate) cho SAKI. Chỉ chứa kỹ năng, kinh nghiệm, độ tuổi (không có tên thật) để tuân thủ quy định bảo mật trước khi chốt hợp đồng. | `mst_moto_company`, `t_dispatch_estimate_response`, `mst_staff` | — |
| `mst_moto_contract_company` | Pháp nhân / đơn vị đại diện phía MOTO dùng để ký hợp đồng. | `mst_moto_company` | `cfg_moto_contract_text_per_client` |
| `cfg_moto_contract_default` | Cấu hình mặc định cho hợp đồng phía MOTO. | `mst_moto_company` | — |
| `cfg_moto_contract_additional_text` | Văn bản bổ sung mặc định trên hợp đồng MOTO. | `mst_moto_company` | — |
| `cfg_moto_contract_text_per_client` | Văn bản hợp đồng tùy biến theo từng khách hàng SAKI. | `mst_moto_company`, `mst_moto_contract_company`, `cfg_moto_client_company` | — |
| `cfg_moto_contract_party` | 契約先企業マスタ — Alias tên hiển thị SAKI trên PDF hợp đồng. MOTO định nghĩa N entries per SAKI customer (`契約先コード` + name JP/EN). `t_contract.contract_party_code` snapshot text từ bảng này. Spec: `docs/superpowers/specs/2026-06-29-cfg-moto-contract-party-design.md`. | `mst_moto_company`, `cfg_moto_client_company` | `t_contract` (logical FK qua `contract_party_code`) |
| `cfg_moto_invoice_default` | Cấu hình mặc định cho hóa đơn phía MOTO. | `mst_moto_company` | — |
| `cfg_moto_invoice_calculation` | Cấu hình quy tắc tính toán hóa đơn của MOTO. | `mst_moto_company` | — |
| `cfg_moto_invoice_fraction` | Cấu hình xử lý phần lẻ (làm tròn) khi tính hóa đơn. | `mst_moto_company` | — |
| `cfg_moto_bank_account` | Tài khoản ngân hàng nhận thanh toán của MOTO. | `mst_moto_company` | — |
| `cfg_moto_seal` | Cấu hình con dấu (印鑑) dùng trên chứng từ MOTO. | `mst_moto_company` | `cfg_moto_seal_per_client` |
| `cfg_moto_seal_per_client` | Cấu hình con dấu tùy biến theo từng khách hàng SAKI. | `cfg_moto_client_company`, `cfg_moto_seal` | — |
| `cfg_moto_advance_payment_screen` | Cấu hình màn hình thanh toán tạm ứng (立替) của MOTO theo từng khách hàng SAKI (client), gồm screen_type (invoice適用/非適用) và kỳ áp dụng. | `mst_moto_company`, `cfg_moto_client_company` | — |
| `cfg_moto_36_agreement` | Cấu hình 36協定 MOTO. Thêm: current_document_id (FK→t_36_agreement_document version PDF hiện hành), last_filing_id (FK→t_36_agreement_filing 届出 gần nhất), expiration_warning_sent_at (BR-1505 60-day notice tránh spam). | `mst_moto_company`, `t_36_agreement_document`, `t_36_agreement_filing` | `t_contract_36_agreement`, `t_36_agreement_document`, `t_36_agreement_filing` |
| `cfg_moto_36_agreement_free` | Cấu hình Hiệp định 36 dạng tự do (đặc biệt) của MOTO. | `mst_moto_company` | — |
| `cfg_moto_36_overtime_start` | Cấu hình mốc bắt đầu tính giờ làm thêm theo Hiệp định 36. | `mst_moto_company` | — |
| `cfg_moto_legal_holiday` | Cấu hình ngày nghỉ pháp định (法定休日) của MOTO. | `mst_moto_company` | — |
| `cfg_moto_compensatory_holiday` | Cấu hình ngày nghỉ bù (振替休日) của MOTO. | `mst_moto_company` | — |
| `cfg_moto_alarm_conflict` | Cấu hình cảnh báo xung đột ngày (抵触日) của MOTO. | `mst_moto_company` | — |
| `cfg_moto_alarm_setting` | Cấu hình cảnh báo chung MOTO. Thêm: channel_email_flg/channel_inapp_flg/is_legal_alert/remind_frequency. CHECK chk_moto_alarm_legal_inapp ép cảnh báo pháp lý (36協定/抵触日) luôn bật in-app (BR-101). | `mst_moto_company` | `cfg_moto_alarm_recipient` |
| `cfg_moto_alarm_recipient` | Người nhận cảnh báo MOTO. Thêm recipient_type (1=ROLE_BASED / 2=SPECIFIC_USER) + CHECK chk_consistency (role_based → recipient_role bắt buộc, user_id NULL; specific_user → user_id bắt buộc). | `cfg_moto_alarm_setting`, `mst_moto_company`, `mst_moto_user` | — |
| `mst_staff` | Hồ sơ nhân viên phái cử (staff) của MOTO — đối tượng trung tâm của chấm công/hợp đồng. | `mst_moto_company` | `mst_staff_conflict_date`, `mst_staff_auth_credential`, `t_attendance_close`, `t_attendance_daily`, `t_contract`, `t_invoice_detail`, `t_moto_shift_schedule`, `t_special_clause_application`, `t_staff_notification_read`, `t_working_time_monthly` |
| `mst_staff_skill` | Bảng lưu trữ danh sách các kỹ năng (Skill) mà nhân viên phái cử (Staff) đang sở hữu cùng với mức độ thành thạo (Level). Phục vụ trực tiếp cho tính năng "CARIMA Matching" để đối chiếu với yêu cầu kỹ năng (`t_dispatch_inquiry_work_skill`) từ phía khách hàng SAKI. | `mst_staff`, `global_master.mst_skill` (Logical FK) | — |
| `mst_staff_qualification` | Bảng quản lý Chứng chỉ / Bằng cấp pháp lý của nhân viên phái cử (như IT, Kế toán, Y tế...) dựa trên `資格マスター.csv`. Bổ trợ cho bảng Kỹ năng để phục vụ Matching chính xác. | `mst_moto_company`, `mst_staff`, `global_master.mst_qualification` (Logical FK) | — |
| `mst_staff_auth_credential` | Thông tin xác thực đăng nhập của staff vào Staff Web; nếu có email thì reset bằng token/OTP email, nếu không có email thì tạo yêu cầu cho MOTO admin xử lý thủ công. | `mst_staff` | `tmp_staff_pwd_reset_token`, `t_staff_password_reset_request`, `t_staff_login_history`, `t_staff_auth_session` |
| `tmp_staff_pwd_reset_token` | Token/OTP quên mật khẩu Staff cho nhánh Staff có email đã đăng ký. | `mst_staff_auth_credential` | — |
| `t_staff_password_reset_request` | Yêu cầu reset mật khẩu Staff cho nhánh Staff không có email; MOTO admin xử lý thủ công và ghi nhận audit bằng trạng thái/request/correlation. | `mst_staff_auth_credential`, `mst_moto_user` | — |
| `t_moto_login_history` | Lịch sử login attempt của user MOTO, bao gồm thành công/thất bại/lock, IP, user-agent, request_id và correlation_id để phục vụ audit/SIEM. | `mst_moto_company`, `mst_moto_user` | — |
| `t_staff_login_history` | Lịch sử login attempt của Staff Web, bao gồm thành công/thất bại/lock, IP, user-agent, request_id và correlation_id. | `mst_moto_company`, `mst_staff_auth_credential` | — |
| `t_moto_auth_session` | Phiên đăng nhập/refresh token hash của user MOTO theo enterprise short session policy; lưu trong tenant schema để giữ tenant isolation. | `mst_moto_company`, `mst_moto_user` | — |
| `t_staff_auth_session` | Phiên đăng nhập/refresh token hash của Staff Web; hỗ trợ revoke khi Staff logout, đổi/reset mật khẩu hoặc MOTO admin reset thủ công. | `mst_moto_company`, `mst_staff_auth_credential` | — |
| `mst_staff_conflict_date` | Ngày giới hạn phái cử / 抵触日 liên quan đến staff, dùng để kiểm soát giới hạn pháp lý trong quá trình phái cử. | `mst_staff` | — |
| `t_contract` | Hợp đồng phái cử (core hệ thống). PK = contract_no (không IDENTITY). MOTO sở hữu vòng đời. RLS theo saki_tenant_id. Revision Model A: superseded_by_id + UNIQUE partial single-active. lock_version optimistic concurrency; draft lifecycle draft_expires_at/expiration_status. | `mst_moto_company`, `mst_staff`, `cfg_moto_client_company`, `t_contract` (self) | `t_36_agreement_monthly`, `t_36_agreement_yearly`, `t_attendance_close`, `t_attendance_close_approval`, `t_attendance_daily`, `t_attendance_daily_approval`, `t_contract` (self), `t_contract_approval`, `t_contract_moto_detail`, `t_contract_saki_detail`, `t_contract_status_history`, `t_invoice_detail`, `t_moto_shift_schedule`, `t_special_clause_application`, `t_working_time_monthly`, `t_attendance_email_history`, `t_contract_document_artifact` |
| `t_contract_moto_detail` | Chi tiết điều kiện hợp đồng do MOTO quản lý, bao gồm đơn giá, quy tắc tính phí, điều kiện thanh toán và các thông tin phục vụ tính hóa đơn. | `t_contract` | — |
| `t_contract_saki_detail` | Chi tiết hợp đồng phía SAKI. Được hệ thống ghi vào schema MOTO thông qua system connection / cross-tenant workflow. | `t_contract` | — |
| `t_contract_work_schedule` | Điều kiện/lịch làm việc hợp đồng (1:1): 業務内容, 責任程度, 勤務日 bitset, giờ làm/nghỉ, OT/休日労働, 更新, 最終延長. M04/M05 đọc để tính toán. | `t_contract` | — |
| `t_contract_billing` | Đơn giá/điều kiện thanh toán hợp đồng (1:1): 単価単位, 請求単価, 割増単価×3, 月額上下限/過不足, 交通費. M05 đọc để tính hóa đơn. | `t_contract` | — |
| `t_contract_person_role` | Người phụ trách hợp đồng (1:N) ngoài requestor/confirmer: 営業/責任者/指揮命令/苦情/請求先 + 勤怠承認者1-3 (MOTO & SAKI). Dùng cho scope/filter + routing duyệt chấm công. | `t_contract` | — |
| `t_contract_36_agreement` | Link hợp đồng ↔ 36協定 (1:N ≤4); M04 giám sát giới hạn OT. | `t_contract`, `cfg_moto_36_agreement` | — |
| `t_contract_job_category` | Link hợp đồng ↔ 台帳職種 (1:N ≤3); logical ref `global_master.mst_ledger_job_category`. | `t_contract` | — |
| `t_contract_legal_holiday` | Quy định ngày nghỉ pháp định per-hợp đồng (1:1); M04 tính giờ làm ngày nghỉ. Khác `cfg_moto_legal_holiday` cấp company. | `t_contract` | — |
| `t_contract_approval` | Lịch sử phê duyệt hợp đồng phía SAKI (1:N); lưu saki_tenant_id/saki_user_id dạng logical FK và phải được index. | `t_contract` | — |
| `t_contract_status_history` | Nhật ký thay đổi trạng thái hợp đồng, lưu trạng thái trước/sau, actor_type, actor_id, actor_tenant_id, actor_company_code, request_id, correlation_id và lý do thay đổi; actor có thể là MOTO user, SAKI logical user hoặc system. | `t_contract` | — |
| `t_contract_termination_detail` | Chi tiết pháp lý khi chấm dứt hợp đồng (1:1 với contract_type=4). Ghi `termination_reason_type`, `notice_date` (CHECK bắt buộc khi MOTO đơn phương), `agreed_termination_date`. | `t_contract` | — |
| `t_contract_document_artifact` | Metadata file hợp đồng đã generate/version; lưu storage_key, file_hash, version, generated_at, locked_flg, source status. File nằm ở S3, DB chỉ lưu metadata bất biến theo version. | `t_contract` | — |
| `t_moto_contract_upload_history` | Lịch sử upload file (TSV) xác nhận/đăng ký hợp đồng hàng loạt. | `mst_moto_company`, `mst_moto_user` | — |
| `t_moto_shift_upload_history` | Lịch sử MOTO upload/download シフト表 (BR-1547 audit cả 2 chiều). operation_type 1=UPLOAD/2=DOWNLOAD discriminator. CHECK chk_s3_consistency: UPLOAD bắt buộc s3_storage_key; DOWNLOAD nullable. Downstream INSERT vào `t_moto_shift_schedule`. | `mst_moto_company`, `mst_moto_user` | — |
| `t_treatment_notification` | Thông báo điều kiện đãi ngộ (待遇情報提供) do SAKI cung cấp cho MOTO. Immutable sau SENT — version+superseded_by_id (BR-054). Dual RLS: SAKI theo saki_tenant_id, MOTO theo tenant_id. | `mst_moto_company`, `cfg_moto_client_company` | `t_treatment_notification_contract`, `t_notification_office_conflict`, `t_notification_individual_conflict` |
| `t_treatment_notification_contract` | Link M:N giữa thông báo đối ngộ và hợp đồng (BR-055: 1 notification → nhiều contract). F-0507 gate check dùng bảng này. | `t_treatment_notification`, `t_contract` | — |
| `t_notification_office_conflict` | Audit record — SAKI thông báo ngày xung đột cấp sự nghiệp (事業所単位) cho MOTO. Immutable. Thêm `saki_tenant_id` (RLS) và `notification_level`. | `t_treatment_notification` | — |
| `t_notification_individual_conflict` | Audit record — SAKI thông báo ngày xung đột cấp cá nhân (個人単位) cho MOTO. Immutable. Dual RLS. | `t_treatment_notification`, `mst_staff` | — |
| `t_office_conflict_tracking` | Batch-computed live state 事業所単位抵触日 (1x/ngày). SCD Type 2 khi Cooling Period reset. Tính `warning_level` cho F-0505 cảnh báo 3 mốc. | `mst_moto_company` | `t_office_dispatch_extension` |
| `t_individual_conflict_tracking` | Batch-computed live state 個人単位抵触日 per staff+org_unit (1x/ngày). SCD Type 2. Không có gia hạn (BR-061). | `mst_moto_company`, `mst_staff` | — |
| `t_office_dispatch_extension` | Ghi nhận ý kiến 過半数代表者 khi gia hạn 事業所単位 (BR-057/BR-058). Immutable. result=0 chặn gia hạn. | `t_office_conflict_tracking` | — |
| `t_attendance_daily` | Chấm công ngày của staff; vùng I/O lớn nhất của tenant MOTO, dùng để Staff Web khai báo và SAKI duyệt theo hợp đồng. RLS theo saki_tenant_id. | `mst_staff`, `t_contract`, `mst_moto_attendance_category` | `t_attendance_daily_approval`, `t_invoice_detail_attendance_daily` |
| `t_attendance_daily_approval` | Lịch sử SAKI duyệt chấm công ngày; lưu saki_tenant_id/saki_user_id dạng logical FK và phải được index. | `t_attendance_daily`, `t_contract` | — |
| `t_attendance_close` | Chốt chấm công cuối kỳ. RLS theo saki_tenant_id. | `mst_staff`, `t_contract` | `t_attendance_close_approval` |
| `t_attendance_close_approval` | Lịch sử SAKI duyệt chốt chấm công cuối kỳ; lưu saki_tenant_id/saki_user_id dạng logical FK và phải được index. | `t_attendance_close`, `t_contract` | — |
| `t_working_time_monthly` | Tổng hợp thời gian làm việc theo tháng (batch tổng hợp từ chấm công ngày). | `mst_staff` | `t_36_agreement_monthly` |
| `t_36_agreement_monthly` | Giám sát 36協定 theo tháng. warning_level mở rộng 3→7 tiers (0=NORMAL/1=50%/2=60%/3=70%/4=80%CAUTION/5=90%DANGER/6=100%VIOLATION) phục vụ BR-1513 phân tầng cảnh báo. | `t_working_time_monthly` | `t_36_agreement_yearly` |
| `t_36_agreement_yearly` | Giám sát 36協定 theo năm. warning_level 7 tiers; +special_clause_count (CHECK ≤6 BR-1504 tuyệt đối), +annual_period_start_date snapshot từ cfg_moto_36_agreement.annual_start_date. | `t_36_agreement_monthly` | — |
| `t_special_clause_application` | Đơn xin áp dụng 特別条項. Thêm: approval workflow (approval_status PENDING/APPROVED/REJECTED/AUTO_APPROVED + approved_by/approved_at + rejection_reason CHECK), annual_application_sequence (CHECK ≤6 BR-1504), expiration_date. | `mst_staff`, `t_contract`, `mst_moto_user` | — |
| `t_36_agreement_document` | PDF versioning cho 36協定 (CMP-001). UNIQUE (agreement_id, document_version). is_finalized=1 + evidence_timestamp_id (logical cross-schema → platform.t_evidence_timestamp) cho TSA-signed; superseded_by_id chain khi gia hạn. CHECK file_size ≤10MB, hash SHA-256 format. | `cfg_moto_36_agreement`, `mst_moto_user`, self (superseded) | `cfg_moto_36_agreement` (current_document_id), `t_36_agreement_filing` |
| `t_36_agreement_filing` | 届出 government submission tracking. submission_status PENDING/SUBMITTED/ACKNOWLEDGED/REJECTED/WITHDRAWN, labor_office_name + jurisdiction_code, receipt_number BR-1503 (CHECK chk_acknowledged_consistency: status=ACKNOWLEDGED phải có receipt). submission_method ONLINE/PAPER/POSTAL. | `cfg_moto_36_agreement`, `t_36_agreement_document`, `mst_moto_user`, self (resubmit chain) | `cfg_moto_36_agreement` (last_filing_id) |
| `t_36_alarm_threshold_state` | Lịch sử transition 6 tier (50/60/70/80/90/100%) per (staff, contract, period). UNIQUE chống duplicate cùng tier per period. BR-1513 phân tầng: tier 4 (80%) gửi 1 lần/kỳ; tier 5/6 (90%+) hàng ngày — last_notification_sent_at + notification_count tracking tránh spam. | `mst_staff`, `t_contract` | — |
| `t_moto_ledger_manual_data` | 派遣元管理台帳 Article 37 manual fields per (contract, staff) — UNIQUE. JSONB display-only: training_records/career_consulting/complaint_handling/capability_evaluation/health_check (≥5 fields per item, không filter individual). Retention 3 năm; PDF export tĩnh S3 12年4ヶ月. | `mst_staff`, `t_contract`, `mst_moto_company` | — |
| `t_attendance_email_history` | Chỉ phục vụ email Chấm công (ALERT_36, APPROVAL_REMINDER) — read-only freeze. Universal email mới ghi vào `t_moto_email_send_history` (BR-109/110/111). Dữ liệu cũ giữ retention 1 năm. | `mst_moto_company`, `t_contract` | — |
| `t_invoice` | Header hóa đơn gửi SAKI theo mô hình Immutable Versioning. SAKI có thể approve/reject/request correction qua cross-tenant workflow; MOTO reissue bằng cách insert version mới, không ghi đè version cũ. RLS theo saki_tenant_id. Tax grouping → `t_invoice_amount_summary`. Lock/close state → `t_invoice_state`. | `mst_moto_company`, `cfg_moto_client_company`, `t_invoice` (self) | `t_invoice_approval`, `t_invoice_amount_summary`, `t_invoice_bank_account`, `t_invoice_detail`, `t_invoice_document_artifact`, `t_invoice_state`, `t_invoice_status_history`, `t_invoice` (self) |
| `t_invoice_state` | Trạng thái vận hành mutable của nhóm hóa đơn (1:1 per invoice_group_id): lock/修正不可/月次締め/取消. Tách riêng khỏi `t_invoice` (immutable) để tránh bug khi reissue. | `t_invoice` (group) | — |
| `t_invoice_amount_summary` | Tổng tiền phân theo nhóm thuế suất (10%/8%/非課税) — thay thế flat columns trên `t_invoice`. Snapshot immutable theo invoice_id (version cụ thể). | `t_invoice` | — |
| `t_invoice_approval` | Lịch sử quyết định của SAKI đối với từng version hóa đơn: APPROVE, REJECT, REQUEST_CORRECTION; lưu logical actor SAKI, reason, request_id và correlation_id. | `mst_moto_company`, `t_invoice` | — |
| `t_invoice_status_history` | Audit trạng thái hóa đơn, ghi nhận chuyển source_status/decision_status khi MOTO issue/reissue/withdraw hoặc SAKI approve/reject/request correction. | `mst_moto_company`, `t_invoice` | — |
| `t_invoice_document_artifact` | Metadata file hóa đơn đã generate/version; lưu storage_key, file_hash, version, generated_at, locked_flg, source status. File nằm ở S3, DB chỉ lưu metadata bất biến theo version. | `t_invoice` | — |
| `t_invoice_bank_account` | Snapshot tài khoản nhận thanh toán in trên hóa đơn tại thời điểm phát hành, tối đa 6 tài khoản. | `t_invoice` | — |
| `t_invoice_detail` | Chi tiết hóa đơn theo từng nhân viên/kỳ. | `mst_staff`, `t_contract`, `t_invoice` | `t_invoice_adjustment_detail`, `t_invoice_detail_attendance_daily`, `t_invoice_reimbursement_header` |
| `t_invoice_detail_attendance_daily` | Chi tiết từng ngày chấm công ánh xạ vào dòng hóa đơn (1:N, phục vụ in chứng từ). | `t_invoice_detail` | — |
| `t_invoice_adjustment_detail` | Các khoản điều chỉnh cộng/trừ trên hóa đơn (1:N). | `t_invoice_detail` | — |
| `t_invoice_reimbursement_header` | Header nhóm khoản thanh toán hộ/tạm ứng (立替金) liên kết với dòng hóa đơn; có nhiều detail theo hạng mục chi phí. | `t_invoice_detail` | `t_invoice_reimbursement_detail` |
| `t_invoice_reimbursement_detail` | Chi tiết khoản thanh toán hộ theo hạng mục chi phí (1:N); lưu snapshot expense category code/name tại thời điểm lập hóa đơn. | `t_invoice_reimbursement_header` | — |
| `t_invoice_upload_history` | Lịch sử upload file CSV liên quan hóa đơn phía MOTO. | `mst_moto_company`, `mst_moto_user` | `t_invoice` |
| `t_document_download_audit` | Audit tải/xem tài liệu hợp đồng/hóa đơn; lưu document_type, document_artifact_id dạng logical reference tới artifact tương ứng, actor_type, actor_id, ip_address, user_agent, request_id, correlation_id, downloaded_at. | `mst_moto_company` | — |
| `mst_moto_shift` | Master ca làm việc do MOTO định nghĩa (giờ bắt đầu/kết thúc, nghỉ — lưu bằng phút). | `mst_moto_company` | `t_moto_shift_schedule` |
| `t_moto_shift_schedule` | Lịch ca dự kiến (シフト表) của staff theo hợp đồng. RLS theo saki_tenant_id. Khác `t_work_schedule`. | `mst_moto_shift`, `mst_staff`, `t_contract` | — |
| `t_work_schedule` | Lịch làm việc dự kiến (勤務予定) đưa vào chấm công; có 区分 + vòng đời 申請/承認 (8 trạng thái) + 締め単位. KHÁC シフト表. | `mst_staff`, `t_contract` | — |
| `t_attendance_reimbursement` | Tiền tạm ứng (立替金) theo ngày của staff; gom vào hóa đơn M05. Khác `t_invoice_reimbursement_*`. | `mst_staff`, `t_contract` | `t_attendance_reimbursement_detail` |
| `t_attendance_reimbursement_detail` | Chi tiết 立替金 ngày (1:N): 業務交通費/その他, 課税区分, インボイス, file đính kèm. | `t_attendance_reimbursement` | — |
| `t_staff_notification_read` | Trạng thái đã đọc notification của Staff Web; tham chiếu logic tới t_platform_notification_history và lưu read_at theo từng staff. | `mst_staff` | — |
| `t_moto_batch_execution_history` | Lịch sử async job của MOTO (tổng hợp tháng, Hiệp định 36, tạo hóa đơn), bao gồm job hệ thống và job/export do user thực hiện nếu có. | `mst_moto_company`, `mst_moto_user` | — |
| `t_moto_audit_log` | Audit log nghiệp vụ/security của MOTO tenant. Lưu đầy đủ cross-tenant actor metadata (actor_tenant_id, actor_company_code) bên cạnh actor_type, actor_id, request_id, correlation_id phục vụ hệ thống SIEM. DB giữ 6 tháng. | — | — |
| `t_moto_company_scheduled_change` | Thay đổi `mst_moto_company` đã lên lịch hiệu lực tương lai (適用日予約, parity e-staffing 企業マスタ). apply theo `effective_date` → update company master + ghi audit-log. Tối đa 1 PENDING/công ty (partial-unique). History dùng audit-log, không versioning master row. | `mst_moto_company`, `mst_moto_user` | `t_moto_audit_log` (logical — apply ghi history) |
| `t_moto_inbox_event` | Event MOTO nhận. Cấu trúc lưu source_tenant_type, source_tenant_id (0 = PLATFORM/SYSTEM), aggregate_type, aggregate_id, payload. UNIQUE theo company_id, source_type, source_id, idempotency_key để chống trùng lặp. | `mst_moto_company` | — |
| `t_moto_outbox_event` | Event MOTO phát sinh. Cấu trúc lưu target_tenant_type, target_tenant_id (0 = PLATFORM/BROADCAST), aggregate_type, aggregate_id, payload. UNIQUE theo company_id, target_type, target_id, idempotency_key. | `mst_moto_company` | — |
| `t_moto_upload_file_result` | Kết quả xử lý file upload của MOTO khi có lỗi; lưu upload_history_id dạng logical reference tới upload history tương ứng, result_file_key, error_summary. Không tạo bản ghi lỗi nếu upload thành công hoàn toàn. | — | — |
| `t_moto_notification_read` | Trạng thái đã đọc notification của user MOTO; tham chiếu logic tới t_platform_notification_history và lưu read_at theo từng user (お知らせ broadcast). | `mst_moto_user` | — |
| `t_moto_notification_log` | Sổ cái Thông báo MOTO — audit master immutable insert-only cho cả MOTO User VÀ Staff (cùng tenant). `audience_scope` phân biệt MOTO_USER_ONLY/STAFF_ONLY/BOTH. Snapshot subject/body (D5 + 電子帳簿保存法). Retention 3 năm (compliance). | `mst_moto_company`, `mst_notification_template` (logical cross-schema) | `t_moto_inbox_notification`, `t_staff_inbox_notification`, `t_moto_email_send_history` |
| `t_moto_inbox_notification` | Hộp thư MOTO user per-user (fan-out at write). Retention 90 ngày, batch hard-delete. Partial index unread+user_id cho badge count nhanh (BR-104). | `mst_moto_company`, `mst_moto_user`, `t_moto_notification_log` | — |
| `t_staff_inbox_notification` | Hộp thư Staff Web per-staff. RLS scope theo staff_id (Staff không dùng System User — Staff App độc lập). Retention 90 ngày. Cùng schema MOTO vì Staff thuộc data ownership của tenant MOTO. | `mst_moto_company`, `mst_staff`, `t_moto_notification_log` | — |
| `t_moto_email_send_history` | Universal email send history MOTO — thay thế việc mở rộng `t_attendance_email_history` (DEPRECATED). Cover mọi loại email (alarm/contract/invoice/treatment/attendance/staff). SES delivery tracking + retry 3 lần (BR-110). recipient_type 1=MOTO_USER/2=STAFF/3=EXTERNAL. Retention 1 năm (BR-109 + 電子帳簿保存法). | `mst_moto_company`, `mst_moto_user`, `mst_staff`, `t_moto_notification_log` | — |

---